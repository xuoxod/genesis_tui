// Grid & Spatial defaults
pub const DEFAULT_GRID_WIDTH: f32 = 200.0;
pub const DEFAULT_GRID_HEIGHT: f32 = 100.0;

// Temporal constraints
pub const DEFAULT_TICK_RATE_MS: u64 = 16; // ~60 FPS
pub const MAX_TICK_RATE_MS: u64 = 500; // Slowest possible speed
pub const MIN_TICK_RATE_MS: u64 = 5; // Absolute maximum speed

pub const MAX_FOSSIL_RECORD_SIZE: usize = 1000;
